import FirebaseAdapter from 'emberfire/adapters/firebase';

export default FirebaseAdapter.extend({
});

//adaptor file connect our application to their data source
//this code Expor default FirebaseAdapter connects to firebase
